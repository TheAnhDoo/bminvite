
  # BM Invite Tool Layout

  This is a code bundle for BM Invite Tool Layout. The original project is available at https://www.figma.com/design/bJ3VcYPTTblqv77qJg3cdr/BM-Invite-Tool-Layout.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  